module assert

// util module to assert that the current term matches what we expect
imports Test
imports libstrc // Includes stratego ast
  
strategies
	  
assert-module =
 	(?Module(_,_) <+ debug(!"Assert: current term is not a Module"); fail)
 	
  echo(|msg):
  	t -> t
  	where
  		<debug> msg